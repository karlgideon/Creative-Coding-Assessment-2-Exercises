function setup() {
  createCanvas(400, 400);
  background(48, 25, 52);
}

function draw() {
  
  noFill()
  stroke(255, 192, 153)
  circle(mouseX, mouseY, 50)
}

function mousePressed()
{
  background(48, 25, 52)
}